version: '3.5'

services:
  db:
    image: postgres:14.3-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_DB: keeper_test
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 12345
